type Address = {
    city: string;
};

export type User = {
    id: string;
    username: string;
    firstName: string;
    lastName: string;
    gender: string;
    eyeColor: string;
    university: string;
    address: Address;
    image: string;
};
